{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm --prefix backend install --include=dev && npm --prefix backend exec prisma generate --schema backend/prisma/schema.prisma && npm --prefix backend exec tsc -p backend/tsconfig.json"
  },
  "deploy": {
    "startCommand": "npm --prefix backend run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
