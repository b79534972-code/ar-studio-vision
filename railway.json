{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm --prefix backend install && npx prisma generate --schema backend/prisma/schema.prisma && npx tsc -p backend/tsconfig.json"
  },
  "deploy": {
    "startCommand": "npm --prefix backend run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
